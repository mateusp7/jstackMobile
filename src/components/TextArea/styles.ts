import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textArea: {
    borderWidth: 1,
    borderColor: '#888',
    borderRadius: 8,
    color: '#333',
    paddingHorizontal: 16,
    paddingTop: 16,
    paddingBottom: 16,
    minHeight: 72,
    maxHeight: 144,
  },
  textAreaFocused: {
    borderColor: '#222',
  },
});
